import React from 'react';
import { useAppStore } from '../stores/appStore';
import { Activity, Heart, Brain, Clock, TrendingUp, AlertCircle } from 'lucide-react';

export const DashboardScreen: React.FC = () => {
  const { user, toggleChat, messages } = useAppStore();

  const stats = [
    { label: 'Health Score', value: '92%', icon: Activity, color: 'from-green-500 to-emerald-500' },
    { label: 'Heart Rate', value: '72 bpm', icon: Heart, color: 'from-red-500 to-pink-500' },
    { label: 'Stress Level', value: 'Low', icon: Brain, color: 'from-purple-500 to-indigo-500' },
    { label: 'Last Check', value: '2h ago', icon: Clock, color: 'from-blue-500 to-cyan-500' },
  ];

  const recentActivity = [
    { type: 'chat', message: 'Discussed chest pain symptoms', time: '2 hours ago', status: 'completed' },
    { type: 'analysis', message: '6 AI agents analyzed your case', time: '2 hours ago', status: 'completed' },
    { type: 'report', message: 'Downloaded health report PDF', time: '1 day ago', status: 'completed' },
  ];

  return (
    <div className="min-h-screen pt-24 pb-12 px-4">
      <div className="container mx-auto max-w-7xl">
        {/* Welcome Header */}
        <div className="mb-8">
          <h1 className="text-4xl font-bold text-white mb-2">
            Welcome back, {user?.name || 'Guest'}! 👋
          </h1>
          <p className="text-gray-400">
            Here's your health overview and recent activity
          </p>
        </div>

        {/* Stats Grid */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
          {stats.map((stat, i) => {
            const Icon = stat.icon;
            return (
              <div key={i} className="glass-morphism rounded-2xl p-6 hover:shadow-neon transition-all">
                <div className="flex items-center justify-between mb-4">
                  <div className={`w-12 h-12 rounded-xl bg-gradient-to-br ${stat.color} flex items-center justify-center`}>
                    <Icon className="w-6 h-6 text-white" />
                  </div>
                  <TrendingUp className="w-5 h-5 text-green-500" />
                </div>
                <div className="text-3xl font-bold text-white mb-1">{stat.value}</div>
                <div className="text-gray-400 text-sm">{stat.label}</div>
              </div>
            );
          })}
        </div>

        {/* Main Content Grid */}
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
          {/* Chat Widget */}
          <div className="lg:col-span-2 glass-morphism rounded-2xl p-6">
            <div className="flex items-center justify-between mb-6">
              <h2 className="text-2xl font-bold text-white">AI Chat Assistant</h2>
              <button
                onClick={toggleChat}
                className="px-4 py-2 bg-gradient-to-r from-primary to-secondary rounded-lg text-white text-sm hover:shadow-neon transition-all"
              >
                Open Full Chat
              </button>
            </div>

            {/* Chat Preview */}
            <div className="space-y-4 mb-4 max-h-80 overflow-y-auto">
              {messages.slice(-5).map((message) => (
                <div
                  key={message.id}
                  className={`flex ${message.role === 'user' ? 'justify-end' : 'justify-start'}`}
                >
                  <div
                    className={`max-w-[80%] rounded-2xl px-4 py-2 ${
                      message.role === 'user'
                        ? 'bg-gradient-to-r from-primary to-secondary text-white'
                        : 'bg-white/5 text-gray-100'
                    }`}
                  >
                    <p className="text-sm">{message.content}</p>
                  </div>
                </div>
              ))}
            </div>

            <button
              onClick={toggleChat}
              className="w-full py-3 glass-morphism rounded-lg text-gray-300 hover:text-white hover:shadow-neon transition-all"
            >
              Start New Conversation
            </button>
          </div>

          {/* Recent Activity */}
          <div className="glass-morphism rounded-2xl p-6">
            <h2 className="text-2xl font-bold text-white mb-6">Recent Activity</h2>
            <div className="space-y-4">
              {recentActivity.map((activity, i) => (
                <div key={i} className="flex items-start space-x-3 p-3 bg-white/5 rounded-lg">
                  <div className="w-8 h-8 rounded-full bg-gradient-to-br from-primary to-secondary flex items-center justify-center flex-shrink-0">
                    {activity.type === 'chat' && '💬'}
                    {activity.type === 'analysis' && '🔬'}
                    {activity.type === 'report' && '📄'}
                  </div>
                  <div className="flex-1 min-w-0">
                    <p className="text-white text-sm font-medium">{activity.message}</p>
                    <p className="text-gray-400 text-xs mt-1">{activity.time}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* Quick Actions */}
        <div className="mt-8 grid grid-cols-1 md:grid-cols-3 gap-6">
          <button className="glass-morphism rounded-2xl p-6 text-left hover:shadow-neon transition-all group">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-cyan-500 flex items-center justify-center mb-4 group-hover:scale-110 transition-transform">
              <Activity className="w-6 h-6 text-white" />
            </div>
            <h3 className="text-lg font-bold text-white mb-2">Run Health Check</h3>
            <p className="text-gray-400 text-sm">Get instant AI assessment</p>
          </button>

          <button className="glass-morphism rounded-2xl p-6 text-left hover:shadow-neon transition-all group">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-purple-500 to-pink-500 flex items-center justify-center mb-4 group-hover:scale-110 transition-transform">
              <Brain className="w-6 h-6 text-white" />
            </div>
            <h3 className="text-lg font-bold text-white mb-2">View AI Insights</h3>
            <p className="text-gray-400 text-sm">See detailed analysis</p>
          </button>

          <button className="glass-morphism rounded-2xl p-6 text-left hover:shadow-neon-red transition-all group">
            <div className="w-12 h-12 rounded-xl bg-gradient-to-br from-red-500 to-orange-500 flex items-center justify-center mb-4 group-hover:scale-110 transition-transform pulse-emergency">
              <AlertCircle className="w-6 h-6 text-white" />
            </div>
            <h3 className="text-lg font-bold text-white mb-2">Emergency Help</h3>
            <p className="text-gray-400 text-sm">Get immediate assistance</p>
          </button>
        </div>
      </div>
    </div>
  );
};
